import 'package:flutter/material.dart';

class ExamTimePageZF extends StatelessWidget {
  const ExamTimePageZF({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("考试时间")),
    );
  }
}
